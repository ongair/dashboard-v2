$(document).ready(function() { 	
  $('.icon_tabs .item').tab();

  $('.icon_tabs').first()
  //.sidebar('attach events', '.toggle.button')
  ;

  $('.toggle.button')
    .removeClass('disabled')
  ;
  
});